/**
 * @module Provides a nice demo / test component 
 */
import * as React from 'react';
import * as gls from './gls';

export const Demo = () => {
  return <gls.FlexHorizontalMargined margin={15} style={{backgroundColor: 'red'}}>
    
  </gls.FlexHorizontalMargined>
}